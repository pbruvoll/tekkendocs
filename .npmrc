; TypeScript 6.0 is newer than the `typescript@^5.1.0` peerOptional range
; declared by @react-router/* 7.11.0. TS 6.0 is compatible, so relax npm's
; strict peer resolution to allow the install.
legacy-peer-deps=true
